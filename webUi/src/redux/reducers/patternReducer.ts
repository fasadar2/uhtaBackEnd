import { createReducer } from "@reduxjs/toolkit";
// import { loadPattern } from "../actions/PatternActions";

export const patternReducer = createReducer(false, (builder) => {
  //   builder.addCase(loadPattern, (state, action: PayloadAction<any>) => state);
});
